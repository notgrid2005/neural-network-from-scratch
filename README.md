# 🧠 Neural Network From Scratch

A complete neural network implementation built entirely from scratch using only NumPy. Features custom backpropagation, multiple activation functions (ReLU, Sigmoid, Tanh), and optimization algorithms (SGD, Adam).

## Features
- No ML frameworks used
- Extensible layer architecture
- Mini-batch gradient descent
- Cross-entropy and MSE loss functions